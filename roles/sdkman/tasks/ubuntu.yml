---
- name: install curl
  become: yes
  apt:
    name:
      - curl
    state: latest
    update_cache: yes

- name: install sdkman
  shell: >
    curl -s "https://get.sdkman.io" | bash
  args:
    creates: "{{ ansible_env.HOME }}/.sdkman"
